<UserControl x:Class="WpfApplication.Views.ProductViewList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <StackPanel Orientation="Vertical" Margin="10">
            <Button Content="⬅ Back" Width="80" Click="BackButton_Click"/>
            <ListView x:Name="ProductsViewList" Margin="0,10,0,0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="{Binding ProductName}" Width="120"/>
                            <TextBlock Text="{Binding Category}" Width="100"/>
                            <TextBlock Text="{Binding Manufacturer}" Width="100"/>
                            <TextBlock Text="{Binding ProductPrice}" Width="60"/>
                            <Button Content="Remove" Click="RemoveProduct_Click"/>
                        </StackPanel>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackPanel>
    </Grid>
</UserControl>